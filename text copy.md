> 第一次分割库与模块： 从“(?<lib>\S+)”引入(?<pkgs>\S+)。
> 本文档有未实现的功能，请不要使用

从“数学2”引入根号2，平方2。
从“数学”引入根号，平方。

> 评论正则：^>\s

# 命名空间
> 正则：^#\s

## 类型：素数
## 类型：合数

## 方法：判断【输入数】是否是【测试】素数
> 正则^#{2}\s
> 调用正则：判断[\S]+是否是素数

若输入数等于1，则确定为否，终止。
若输入数等于2，则确定为是，终止。
若输入数等于3，则确定为是，终止。

> 赋值正则：令(?<front>[\S]+)为(?<end>[\S]+)[。，]

令判断上界为根号输入数。
从2到判断上界的每个整数：令余数为输入数%它，若0等于余数，则确定为否，终止。
确定为是。

> 它为最近一个声明的，并且在句子中。
> 确定为相当于为返回值赋值，但如果不终止，仍会继续执行，并且忽略接下来的确定为。

## 运行
> 判断6是否是素数，若它是，则输出“是素数”，否则输出“是猪”。

> 匹配列表项： \*\s(?<item>\S+)\n
> 匹配列表体：\n(\*\s\S+\n)+(结束)。

从1到5的每个整数：
* 令结果为判断它是否是0素数。
* 若结果是，则输出整数，输出“这也是素数”。
* 若结果不是，则输出整数，输出“这不是”。
> * 从2到3的每个整数二：
>     * 输出整数二。
> * 结束。
结束。

若100小于2：
* 输出“y es”。
否则若100小于8：
* 输出“n no”。
否则：
* 输出“n no2”。
结束。


